import { createContext } from "react";


export const ChatInboxSocketContext:any=createContext({})
export const NavbarSocketContext:any=createContext({})
export const InboxSocketContext:any=createContext({})